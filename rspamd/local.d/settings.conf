authenticated {
  priority = high;
  authenticated = yes;
  apply {
    actions {
        "add header" = null;
    }
  }
}

local {
  priority = high;
  local = yes;
  apply {
    actions {
        "add header" = null;
    }
  }
}
